# Публичный URL вашего API на Render (без / в конце, без /api).
# Пример: https://grand-astra-api.onrender.com
#
# Сборка: переменная должна быть задана на этапе build (Static Site → Environment).
VITE_API_URL=
